# AI-News Backend Python Dependencies
# 后端 Python 脚本所需依赖包

##############################
### Core Dependencies
##############################
# MongoDB 驱动
pymongo>=4.6.0,<5.0.0

# HTTP 请求库
requests>=2.31.0,<3.0.0
urllib3>=1.26.0,<2.0.0

# HTML/XML 解析
beautifulsoup4>=4.12.0
lxml>=4.9.0

# RSS/Feed 解析
feedparser>=6.0.10

##############################
### Web Scraping & Browser Automation
##############################
# Playwright 浏览器自动化（用于抓取需要 JavaScript 渲染的页面）
playwright>=1.40.0

# Crawl4AI 爬虫框架（如果使用）
crawl4ai>=0.3.0

##############################
### Data Processing
##############################
# HTML 转 Markdown
html2text>=2024.2.26
markdownify>=0.11.6

# 字符编码检测
chardet>=5.2.0

##############################
### AI/LLM Integration
##############################
# 统一保留 DMAX / OpenRouter 两类远程接口，均通过 requests 调用
# 如后续接入新的模型供应商，再按需补充

# 重试机制
tenacity>=8.2.0
backoff>=2.2.0

##############################
### Utilities
##############################
# 日期时间处理
python-dateutil>=2.8.0

# 系统监控（如果使用）
psutil>=5.9.0

# Flask（如果使用 SystemMonitorService）
flask>=2.3.0
pika>=1.3.2
APScheduler>=3.10.4

# Pydantic（用于数据验证）
pydantic>=2.0.0

# 环境变量管理
python-dotenv>=1.0.0

# 加密库（如果需要）
cryptography>=41.0.0

# JSON 修复
json-repair>=0.7.0

##############################
### Development & Testing (可选)
##############################
# pytest>=7.4.0
# pytest-asyncio>=0.21.0

##############################
### Notes
##############################
# 安装 Playwright 浏览器：
#   playwright install
#   或
#   playwright install chromium

# 如果使用 GPU 加速（可选）：
#   onnxruntime-gpu>=1.16.0

# 如果使用向量数据库（可选）：
#   chromadb>=0.4.0
#   sentence-transformers>=2.2.0

