"""Shared Python modules for backend microservices.

This package is the new landing zone for reusable Python service code.
During migration it re-exports selected root modules to avoid breaking imports.
"""
